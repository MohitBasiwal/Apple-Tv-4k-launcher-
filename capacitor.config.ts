import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.frostedtvlauncher',
  appName: 'Frosted TV Launcher',
  webDir: 'dist'
};

export default config;
